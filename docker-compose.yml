services:
  app:
    container_name: ${APP_INSTANCE_NAME}-${NODE_ENV}
    build:
      context: ./
      dockerfile: Dockerfile
    networks:
      - dokploy-network
    labels:
      - traefik.enable=true

networks:
  dokploy-network:
    external: true
